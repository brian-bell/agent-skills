interface:
  display_name: "Fix PR"
  short_description: "Triage unresolved PR comments"
  default_prompt: "Use $fix-pr to gather unresolved GitHub PR comments, classify each as accepted, rejected, or already fixed, and display the report without replying to or resolving comments."
