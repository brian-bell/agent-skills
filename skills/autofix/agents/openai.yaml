interface:
  display_name: "Autofix"
  short_description: "Fix PR comments, review, ship, and reply"
  default_prompt: "Use $autofix --comment <GitHub comment URL> to fix the requested PR feedback, run autoreview, ship the update, and reply to the comment."
